name: vx
description: "An easy-to-use, multi-platform proxy client"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archßive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 3.5.8+2062

environment:
  sdk: ">=3.8.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  json_annotation: ^4.11.0
  sentry_flutter: ^9.12.0
  flutter:
    sdk: flutter
  ffi: ^2.1.2
  go_router: ^14.2.0
  protobuf: ^6.0.0
  tm:
    path: ../tm-plugin/tm
  google_fonts: ^6.2.1
  provider: ^6.1.2
  equatable: ^2.0.5
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  logger: ^2.4.0
  uuid: ^4.4.2
  http: ^1.5.0
  shared_preferences: ^2.3.2
  bitsdojo_window: ^0.1.6
  gap: ^3.0.1
  flutter_svg: ^2.0.10+1
  flutter_swipe_action_cell: ^3.1.5
  freezed_annotation: ^3.0.0
  pasteboard: ^0.4.0
  zxing2: ^0.2.3
  image: ^4.3.0
  path_provider: ^2.1.5
  macos_window_utils: ^1.6.1
  path: ^1.9.0
  percent_indicator: ^4.2.4
  app_links: ^6.4.0
  drift: ^2.26.1
  drift_flutter: ^0.2.4
  tray_manager: ^0.4.0
  window_manager: ^0.5.0
  archive: ^3.6.1
  system_proxy:
    git:
      url: https://github.com/5vnetwork/system-proxy.git
  url_launcher: ^6.3.1
  win32_registry: ^2.1.0
  rxdart: ^0.28.0
  fixnum: ^1.1.1
  installed_apps:
    git:
      url: https://github.com/5vnetwork/installed_apps.git
  package_info_plus: ^8.3.0
  grpc: ^5.1.0
  lru_cache: ^0.0.2
  flutter_secure_storage: ^10.0.0
  vector_graphics: ^1.1.18
  file_picker: ^10.1.9
  convert: ^3.1.2
  qr_flutter: ^4.1.0
  auto_size_text: ^3.0.0
  crypto: ^3.0.6
  cryptography: ^2.7.0
  encrypt: ^5.0.3
  device_info_plus: ^11.5.0
  flutter_staggered_grid_view: ^0.7.0
  supabase_flutter: ^2.9.1
  sign_in_with_apple: ^7.0.1
  google_sign_in: ^7.2.0
  in_app_purchase: 3.2.3
  in_app_review: ^2.0.10
  mobile_scanner: ^7.0.1
  android_package_installer: ^0.0.3
  flutter_local_notifications: ^19.4.0
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.2
  google_api_availability: ^5.0.1
  file_saver: ^0.3.1
  launch_at_startup: ^0.5.1
  fl_chart: ^1.1.1
  collection: ^1.19.1
  pretty_qr_code: ^3.5.0
  # flutter_single_instance: ^1.6.1
  # liquid_glass_renderer: ^0.1.1-dev.10
  # flutter_sparkle:
  #   path: ../flutter_sparkle
  flutter_common:
    path: ../flutter_common
  ads:
    path: ../flutter_common/packages/ads
  firebase_crashlytics: ^5.0.8
  firebase_analytics: ^12.1.3

dev_dependencies:
  sentry_dart_plugin: ^3.2.1
  flutter_launcher_icons: ^0.14.3
  json_annotation: ^4.11.0
  pigeon: ^26.2.3
  mockito: ^5.4.6
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  ffigen: ^18.0.0
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.7.2
  json_serializable: ^6.8.0
  freezed: ^3.1.0
  drift_dev: ^2.26.0
  msix: ^3.16.12
  flutter_oss_licenses: ^3.0.4
  in_app_purchase_platform_interface: ^1.4.0
  flutter_arb_translator: ^1.0.23

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # fonts:
  #   - family: NotoSansSC
  #     fonts:
  #       - asset: assets/google_fonts/NotoSansSC-Regular.ttf
  #       - asset: assets/google_fonts/NotoSansSC-Medium.ttf
  #         weight: 500

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/
    - assets/icons/flags/
    - assets/geo/
    - assets/
    - assets/configs/
    - assets/oss/
    - assets/ads/
    - assets/google_fonts/

  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
msix_config:
  msstore_appId: 9PHBCBZ9R1FX
  display_name: VX
  publisher_display_name: 5V Network
  identity_name: 5VNetwork.XProxy
  msix_version: 3.0.4.0
  logo_path: windows/runner/resources/app_icon.ico
  capabilities: internetClient
  publisher: CN=ABB09CD5-F7BA-4835-BDF5-8CB6DDDCC432
  protocol_activation: vx
  install_certificate: false
  debug: false
  trim_logo: false
  language: zh-cn, en-us
  output_path: build/windows/msix
  windows_build_args: --dart-define=STORE=true
  store: true

sentry:
  upload_debug_symbols: true
  upload_source_maps: true
  project: flutter
  org: 5v-network-llc
